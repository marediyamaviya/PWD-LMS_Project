spring.application.name=identity-service

server.port=${PORT:8081}
server.tomcat.accesslog.enabled=true

# Database (Render Postgres)
spring.datasource.url=${DB_URL:jdbc:postgresql://dpg-daif44bm8hqs73cjqkdg-a.oregon-postgres.render.com/lms_db_ak0i}
spring.datasource.username=${DB_USERNAME:lms_db_ak0i_user}
spring.datasource.password=${DB_PASSWORD:JNOrJkUF380lFjlx8v8lAOZsLbqw82hY}

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# JWT
jwt.secret=${JWT_SECRET:your-very-long-secret-key-change-this-later}
jwt.expiration=86400000

# Eureka Client
eureka.client.service-url.defaultZone=${EUREKA_SERVER:http://localhost:8761/eureka/}
eureka.client.register-with-eureka=true
eureka.client.fetch-registry=true
eureka.instance.hostname=${EUREKA_HOSTNAME:localhost}